import { createDatabase } from '../src/database';
import {
  DynamoDB,
  GetItemCommandInput,
  AttributeValue,
} from '@aws-sdk/client-dynamodb';

describe('Database', () => {
  it('saves an item', async () => {
    console.log('TODO');
  });
});
