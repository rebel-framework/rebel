{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.1.4"
  },
  "scripts": {
    "build": "npx lerna run build",
    "nuke": "rm -rf node_modules",
    "permission": "chmod +x packages/cli/bin/index.js"
  }
}
